import "strings"
import "types"
import "net/http"
import "io/ioutil"

response, err := http.Get("https://odexawwp5c.execute-api.us-west-2.amazonaws.com/default/sentinel-remote")

    if err != nil {
        print(err.Error())
    }

    responseData, err := ioutil.ReadAll(response.Body)
    if err != nil {
        print(err.Error())
    }


# Main rule
main = rule {
  responseData.allow is true
}